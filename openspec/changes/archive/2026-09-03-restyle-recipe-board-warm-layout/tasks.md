## 1. 画面の順とラベル

- [x] 1.1 `frontend/src/pages/DetailPage.tsx` の閲覧で材料ブロックを本文より上にし、本文の見出しを「手順」、空なら見出しの下に「（本文なし）」とし、星・画像・削除は維持することをコードで確認する
- [x] 1.2 同ファイルの編集で材料欄を本文より上にし、本文ラベルを「手順（任意）」にし、保存フィールド名が `body` のままであることをコードで確認する
- [x] 1.3 `frontend/src/pages/NewPage.tsx` でタイトル必須を先頭のまま、材料欄を本文より上、本文ラベルを「手順（任意）」にし、画像ファイル選択（有効時）は材料の後であることをコードで確認する

## 2. 色と行間

- [x] 2.1 `frontend/src/index.css` の `:root` だけを暖色（乳白背景、テラコッタ／柔らかいオレンジのアクセント）に変え、`--star` の金を維持し、コンポーネントライブラリを追加していないことを diff で確認する
- [x] 2.2 `.ingredient-view` の `gap` / `line-height` だけを少し狭くし、`letter-spacing` と `.ingredient-row` のグリッドを大きく変えていないことを CSS で確認する

## 3. ローカル確認（Playwright MCP）

- [x] 3.1 backend を `127.0.0.1:8000`、front を `127.0.0.1:5173` で起動し、Playwright MCP で `/new` の順がタイトル → 材料 → 手順（任意）、ファイル選択が無いこと、暖色であることを確認する
- [x] 3.2 Playwright MCP で詳細閲覧の材料が手順より上、「手順」見出し、空なら「（本文なし）」、編集でも材料が手順より上でラベルが「手順（任意）」、一覧も暖色で星が金のままであることを `127.0.0.1:5173` で確認する

## 4. 触らないこと

- [x] 4.1 `backend/`、`backend/tests/`、`template.yaml`、pytest を変更していないことを git status で確認する
- [x] 4.2 `sam build` / `sam deploy`、本番フロント載せ直し、git commit / push を行っていないことを作業ログと git status で確認する。`AGENTS.md` とメモアプリも触っていないこと
